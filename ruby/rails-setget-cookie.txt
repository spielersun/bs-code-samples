class CookieController < ApplicationController
     def create_cookie
          cookies[:name] = "MURAT"
          redirect_to :action => "show_cookie"
     end
     def show_cookie
          name = cookies[:name]
          render(:text => "Merhaba #{name}!")
     end
end